# Baseline placeholder

No live baseline has been captured yet. The first keyed run of `run_eval.py`
(performed later by the orchestrator, with `REPORT_LLM_API_KEY` set) writes the
reference prose per section here. Until then this file just keeps the `baseline/`
directory under version control.
